import * as maptalks from 'maptalks';
import { ThreeLayer } from './../index';
import * as THREE from 'three';
export declare function vectors2Pixel(worldPoints: Array<number> | Array<THREE.Vector3>, size: maptalks.Size, camera: THREE.Camera, altitude: number, layer: ThreeLayer): Array<{
    x: number;
    y: number;
}>;
export declare function vector2Pixel(world_vector: THREE.Vector3, size: maptalks.Size, camera: THREE.Camera): {
    x: number;
    y: number;
};
